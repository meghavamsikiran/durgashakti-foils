server:
  port: 8014

spring:
  application:
    name: admin-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:db.vddtkiefzhcihdzxxlgp.supabase.co}:${DB_PORT:5432}/${DB_NAME:postgres}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:Kiran@1435}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET:YOUR_STRONG_RANDOM_JWT_SECRET_MIN_48_CHARS}

import React from 'react';

/**
 * PageLoader — Returns nothing. Data loads in the background and
 * renders when ready. The sacred Trishul + Durga Maa loaders are
 * handled exclusively by RouteTransitionLoader on first visit / refresh.
 */
const PageLoader = () => null;

export default PageLoader;
